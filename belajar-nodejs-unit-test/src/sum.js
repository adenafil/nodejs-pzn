export const sum = (first, second) => {
    return first + second;
}

export const sumAll = (numbers) => {
    let total = 0;
    for (numbers of numbers) {
        total += numbers;
    }
    return total;
}